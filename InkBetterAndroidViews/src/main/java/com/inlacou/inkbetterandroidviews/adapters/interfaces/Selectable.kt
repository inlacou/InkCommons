package com.inlacou.inkbetterandroidviews.adapters.interfaces

interface Selectable {
	/**
	 * Represents if the item is selected
	 */
	var selected: Boolean

	/**
	 * Represents if the item is selectable
	 */
	val selectable: Boolean
}